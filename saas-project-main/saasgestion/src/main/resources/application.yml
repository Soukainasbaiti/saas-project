spring:
  application:
    name: SaaSGestion

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  datasource:
    url: jdbc:postgresql://localhost:5432/project_saas?preferQueryMode=simple
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  sql:
    init:
      mode: never

server:
  port: 8080
  servlet:
    context-path: /api

# ── JWT ────────────────────────────────────────────────────────────
# Pas de valeur par defaut pour le secret : l'appli doit refuser de demarrer
# si JWT_SECRET n'est pas explicitement configure (sinon un secret connu/
# public permettrait de forger des tokens valides, y compris ADMIN).
app:
  jwt:
    secret: ${JWT_SECRET}
    access-token-ms: 3600000
    refresh-token-ms: 604800000
  base-url: ${FRONTEND_URL:http://localhost:4200}
  mail:
    from: ${MAIL_FROM:adamadrissa.07@gmail.com}
  brevo:
    api-key: ${BREVO_API_KEY:}

logging:
  level:
    root: INFO
    com.segula.saasgestion: DEBUG