spring:
  datasource:
    url: jdbc:postgresql://${PGHOST:postgres}:${PGPORT:5432}/${POSTGRES_DB:railway}?preferQueryMode=simple
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080

app:
  base-url: ${FRONTEND_URL:https://saas-gestion-front.vercel.app}
  jwt:
    secret: ${JWT_SECRET:segula-saas-super-secret-key-2024-must-be-at-least-32-chars}
    access-token-ms: 3600000
    refresh-token-ms: 604800000
  mail:
    from: ${MAIL_FROM:adamadrissa.07@gmail.com}
  brevo:
    api-key: ${BREVO_API_KEY:}
